# SPDX-License-Identifier: 0BSD
# Short benchmark of the optimized summed runtime path.

schema_version = 1
action = "benchmark"

[evaluation]
artifact = "artifacts/pp_zjj"
process = "p_p_to_z_j_j_4"
momenta = "data/pp_zjj_momenta.json"

[benchmark]
target_runtime = 1.0
batch_size = 128
warmup_runs = 2
minimum_samples = 5
helicity_ids = []
color_flow_ids = []

[output]
format = "json"
progress = "log"
