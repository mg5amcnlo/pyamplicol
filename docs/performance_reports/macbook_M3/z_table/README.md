# Dedicated Z-table checkpoint

This directory is the controlled wrap-up snapshot of the built-in-SM
`d d~ -> Z + gluons` Apple M3 study.

- `z_table.pdf` is the visually inspected four-page report.
- `z_table.tex` and `result_z_builtin_sm_table.tex` are its self-contained
  LaTeX sources.
- `results/z_builtin_sm.json` is the aggregate source cache used to render the
  table; `results/report-cache.schema.json` is its schema.

The measured source for retained rows is
`202b3fd91a40a6e0422320053796fd71b5b2cd48`. Rows `n=1` through `n=7`
retain immutable pre-study evidence, including the corrected prewarmed
original-AmpliCol `n=1` generation time of 2.18 seconds. They keep their
original resource ABI and are not relabelled as Darwin physical-footprint
measurements.

No new study-F contract or `n=8`/`n=9` measurement was launched before this
checkpoint. Those unrun cells remain N/A. ASM O3 and C++ O3 cells above `n=6`
are catalog-declared static N/A and created no attempt directory or compiler
process. Historical `n=8` attempts without the new study binding, worker
harness, and Darwin physical-footprint evidence remain unpublished.

Any future accepted `n=8`/`n=9` study-F attempt must use the reviewed wrapper
policy: a 30,000,000,000-byte limit on the maximum of aggregate process-tree
RSS and Darwin physical footprint, plus a 3,600-second authenticated
generation-phase limit.

The checked-in PDF has SHA-256
`18c32b6dcca97330b8e44bf9e3513f035e79f09e2af2b6d49fc79b4ae6e2c18f`.
