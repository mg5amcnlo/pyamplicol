% SPDX-License-Identifier: 0BSD
\documentclass[10pt,letterpaper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[letterpaper,margin=0.75in,headheight=21pt]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{longtable}
\usepackage{pdflscape}
\usepackage{siunitx}
\usepackage[table]{xcolor}

\DeclareMathSizes{5}{5}{5}{5}
\DeclareMathSizes{6.2}{6.2}{5}{5}

\definecolor{ReportInk}{HTML}{17212B}
\definecolor{ReportBlue}{HTML}{145A86}
\definecolor{ReportGreen}{HTML}{287A58}
\definecolor{ReportOrange}{HTML}{B76E00}
\definecolor{ReportRed}{HTML}{B33A3A}
\definecolor{ReportRule}{HTML}{CBD3DA}
\definecolor{ReportMuted}{HTML}{66717C}
\definecolor{refblue}{HTML}{DCEBFF}
\definecolor{bestgreen}{HTML}{DFF5E4}
\definecolor{speedgreen}{HTML}{1A7F37}
\definecolor{speedorange}{HTML}{BF8700}
\definecolor{speedred}{HTML}{CF222E}

\hypersetup{
  colorlinks=true,
  linkcolor=ReportBlue,
  urlcolor=ReportBlue,
  pdftitle={pyAmpliCol 0.1.0 Methodology and Performance Report},
  pdfauthor={Rikkert Frederix, Valentin Hirschi, and Timea Vitos}
}
\sisetup{detect-all,scientific-notation=true,round-mode=figures,round-precision=3}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.55em}
\setcounter{secnumdepth}{2}
\setcounter{tocdepth}{1}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\textcolor{ReportInk}{pyAmpliCol 0.1.0}}
\fancyhead[R]{\textcolor{ReportMuted}{Methodology and performance}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{ReportRule}\leaders\hrule height \headrulewidth\hfill}}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}
\newcommand{\PAC}{\texttt{pyAmpliCol}}
\newcommand{\AC}{\texttt{AmpliCol}}
\newcommand{\ReportNA}{\textcolor{ReportMuted}{\textsc{n/a}}}
\newcommand{\ReportStatus}[1]{\textcolor{ReportMuted}{\textsc{#1}}}
\newcommand{\ReportMetricCell}[2]{\shortstack{\textcolor{ReportGreen}{G #1}\\\textcolor{ReportBlue}{R #2}}}
\newcommand{\ReportTableNote}[1]{\par\smallskip\noindent\footnotesize\textcolor{ReportMuted}{#1}\par}
\newcommand{\ReportMatrixLegend}{\ReportTableNote{The table heading names the baseline and candidate. LC reports the selected-flow, summed-helicity workload and the all-flow, selected-helicity workload separately. NLC and full colour report one contracted workload. Parenthesized values are candidate-to-baseline ratios; an execution attribution is shown only when both measurements expose it.}}
\newcommand{\ReportLadderLegend}{\ReportTableNote{Each LC workload reports process-generation time in seconds and native wall time in microseconds per phase-space point, together with a separately measured execution attribution when exposed. Compiled rows use the labelled JIT, ASM, or C++ backend; eager and recurrence rows use portable JIT O2 prepared models. Parenthesized runtime values and selected-flow generation values compare each pyAmpliCol row with the corresponding AmpliCol reference workload. All-flow generation shows the absolute pyAmpliCol value marked n.c. because its setup boundary differs from the reference.}}
\newcommand{\ReportLandscapeFooter}{}
\newcommand{\ReportProfileName}{unpopulated}
\newcommand{\ReportPlatformSummary}{specified by the architecture profile}
\newcommand{\ReportToolchainSummary}{specified by the architecture profile}
\newcommand{\ReportEditionStatement}{This source is the unpopulated
  documentation template. Architecture-specific copies contain their recorded
  environment and measured results.}
\InputIfFileExists{report_environment.tex}{}{}

\lstset{
  basicstyle=\ttfamily\small,
  columns=fullflexible,
  frame=single,
  rulecolor=\color{ReportRule},
  breaklines=true,
  showstringspaces=false,
  xleftmargin=0.5em,
  xrightmargin=0.5em
}

\title{\vspace{-1.4cm}\textbf{pyAmpliCol 0.1.0}\\[0.35em]
  \Large Methodology and Performance Report}
\author{Rikkert Frederix \and Valentin Hirschi \and Timea Vitos}
\date{Release 0.1.0\\[0.25em]\small Measurement profile:
  \texttt{\ReportProfileName}}

\begin{document}
\maketitle

\begin{abstract}
This report presents the physics coverage, numerical validation, and
performance of \PAC{} 0.1.0. We compare compiled, eager, and recurrence
evaluation for the built-in Standard Model and for the same model imported from
UFO or serialized JSON, together with scalar-contact and scalar-gravity
benchmarks. Every reported timing is associated with a documented software
revision, hardware profile, numerical configuration, and validation reference
appropriate to the corresponding data set.
\end{abstract}

\begingroup
\small
\setlength{\parskip}{0pt}
\tableofcontents
\endgroup
\clearpage

\section{Scope}

\PAC{} generates and evaluates tree-level scattering amplitudes. Compiled mode
specializes a shared-current directed acyclic graph (DAG) to a process. Eager
mode evaluates the same graph with reusable model kernels. Recurrence mode
constructs a compact current schedule directly. The three modes implement the
same physics normalization, colour treatment, helicity sums, and resolved
observables. The original \AC{} program and its event-generation setting are
described in Ref.~\cite{FrederixVitos2026}.

The report has three purposes:
\begin{enumerate}
  \item define the observables and conditions required for a reproducible
        benchmark;
  \item preserve a stable matrix of representative process families and colour
        approximations; and
  \item present performance only after numerical agreement has been verified.
\end{enumerate}

In each architecture-specific report, an \ReportNA{} entry denotes a
process/multiplicity combination outside the declared scope or an observable
that does not apply. Every applicable cell across the complete declared
multiplicity range must instead contain a validated measurement or an
authenticated resource-limit status. A measurement that does not pass
numerical validation is labelled explicitly and never replaced by a timing
value. No value is copied, interpolated, or estimated from a neighbouring
cell.

\section{Current Recursion and Model Lowering}

\subsection{Shared-current DAG}

For an external subset $I$, particle state $t$, and local spin or flow label
$\chi$, an off-shell current has the schematic recursion
\begin{equation}
  J^{\alpha}_{t,\chi}(I)
  = P^{\alpha}{}_{\beta}(t,p_I)
    \sum_{I=A\mathbin{\dot\cup}B}
    V^{\beta}{}_{\gamma\delta}(t_A,t_B\!\to t)
    J^{\gamma}_{t_A,\chi_A}(A)
    J^{\delta}_{t_B,\chi_B}(B),
  \qquad p_I=\sum_{i\in I}p_i .
  \label{eq:recursion}
\end{equation}
Current identity includes the particle, external labels, helicity ancestry,
chirality or spin state, flavour and charge flow, colour state, momentum mask,
and any auxiliary-current kind. Contributions with the same complete identity
share one node. A topological sweep therefore evaluates each retained current
once per phase-space point and reuses it in all dependent amplitudes.
This is a Berends--Giele-type construction~\cite{BerendsGiele1988}.

The Universal FeynRules Output (UFO) format describes particles, parameters,
interactions, Lorentz tensors, colour tensors, and propagators
in a model-independent form~\cite{UFO2012,UFO2023}. \PAC{} normalizes a trusted
UFO Python module, or its own serialized JSON representation of that model, to
the same canonical representation used by process generation. The UFO module
is executable Python; the corresponding serialized JSON form is data-only.

Optimizations of imported models are based on exact colour, Lorentz,
propagator, and coupling identities rather than particle names or numerical
codes. For Standard-Model validation processes, the built-in and imported
routes must produce equivalent current, interaction, amplitude, and colour
content. Algebraic reuse is allowed only when the relevant identity has been
established exactly.

\subsection{Contact terms and spin two}

A supported vertex with valence above three is lowered to a deterministic tree
of non-propagating auxiliary currents. The construction inserts the original
coupling once and introduces no pole. Direct contraction of the original
contact tensor is the validation reference for the scalar-contact ladder.

The scalar-gravity ladder exercises rank-two external states and
momentum-dependent interactions. For a massless graviton, the helicity-two
polarization tensor is represented as
\begin{equation}
  \epsilon^{\mu\nu}_{\pm 2}
  = \epsilon^{\mu}_{\pm}\epsilon^{\nu}_{\pm} .
\end{equation}
Validation checks symmetry, transversality, tracelessness, normalization, and
agreement between ordinary and higher-precision evaluation.

\section{Colour and Runtime Semantics}

The process matrices use three colour contracts:
\begin{center}
\begin{tabular}{@{}L{0.45in}L{5.85in}@{}}
\textbf{LC} & Leading-colour ordered amplitudes with physical colour-flow selectors~\cite{Maltoni2002}. \\
\textbf{NLC} & Every colour-metric entry whose leading power is at most two below the leading power of the complete basis. \\
\textbf{Full} & The complete supported sparse colour metric for the generated amplitude basis. \\
\end{tabular}
\end{center}

For NLC, ``retained'' applies to a colour-matrix \emph{entry}, not to a
truncated numerical coefficient.  Write the exact overlap of two colour-flow
tensors as
\begin{equation}
  C_{ab}(N_c)=\sum_p c^{(ab)}_p N_c^p,
  \qquad
  p_{ab}=\max\{p:c^{(ab)}_p\ne0\},
  \label{eq:nlc-entry-gate}
\end{equation}
and let \(P\) be the leading power of the complete colour basis.  The sparse
NLC metric is
\begin{equation}
  C^{\mathrm{NLC}}_{ab}=
  \begin{cases}
    C_{ab}(3), & p_{ab}\ge P-2,\\
    0,         & p_{ab}<P-2.
  \end{cases}
  \label{eq:nlc-exact-coefficient}
\end{equation}
Thus every qualifying entry keeps all of its finite-\(N_c\) terms.  For three
open fundamental lines and one adjoint, \(P=4\).  Representative retained
off-diagonal overlaps are

\begin{center}
\begin{tabular}{@{}L{1.45in}L{1.20in}L{1.35in}L{1.35in}@{}}
\toprule
\textbf{exact overlap} & \textbf{highest power} &
  \textbf{NLC at \(N_c=3\)} & \textbf{full at \(N_c=3\)} \\
\midrule
\(-N_c^3+N_c\) & \(3\) & \(-24\) & \(-24\) \\
\(N_c^2-1\) & \(2\) & \(8\) & \(8\) \\
\bottomrule
\end{tabular}
\end{center}

The powers and coefficients are retained exactly.  When only the
upper-triangular part of the colour metric is stored, off-diagonal interference
terms receive the usual factor of two; this factor is not part of the colour
coefficient in Eq.~\eqref{eq:nlc-exact-coefficient}.

The summed observable returns one matrix element per point.  Leading-colour
resolved values have axes \texttt{(point, helicity, flow)}; NLC and full-colour
values have one contracted colour component per helicity. Summing all
non-point axes must reproduce the directly evaluated total. Selectors refer to
physical helicities and flows.

All binary64 interfaces use the same native numerical core. Cross-interface
agreement therefore checks data conventions, while a separately generated
value is required for an independent physics comparison.

The JIT backend stores a portable SymJIT application for binary64 evaluation;
the receiving host creates code appropriate for its processor. ASM and C++
payloads are target-specific. Evaluation above binary64 precision uses the
retained symbolic evaluator state.

\section{Benchmark Methodology}

\subsection{Configuration contract}

Each measurement begins from an immutable resolved configuration. Unless a
table states otherwise, measurements use a batch size of 128, two warmup runs,
at least five timed samples, a five-second sampling target, and one CPU core.
Compiled process matrices use JIT O3; eager and recurrence matrices use
portable JIT O2 prepared models. Explicitly labelled rows in the dedicated
\(Z\) ladder use JIT O1, JIT O3, ASM O3, or C++ O3. Backend-specific
optimization settings are retained with the measurement record.

Generation time begins with the resolved model input required by an execution
mode and ends with a validated, loadable process representation. Compiled mode
includes process-specific evaluator construction and backend compilation.
Eager and recurrence modes include construction and serialization of their
respective numerical schedules. Preparation of a reusable built-in or UFO/JSON
model bundle occurs before this timer and is therefore excluded from the
process-generation values in the tables.

Runtime measurements use the summed matrix element. The reported native wall
time starts after caller-language array conversion and includes source
construction, numerical evaluation, selector handling, and final reduction.
Caller-language packing is excluded. When a backend exposes a separately
measured execution attribution, the tables show it alongside wall time;
otherwise the field is marked \textsc{not exposed} and is never inferred or
replaced by zero. Comparisons use the same process, colour accuracy,
parameters, phase-space point, precision, selectors, and normalization.

\subsection{Benchmark platform}

\ReportEditionStatement The recorded hardware, operating system, and processor
summary is \ReportPlatformSummary. The measurement profile is
\texttt{\ReportProfileName}; its recorded software environment is
\ReportToolchainSummary. Measurements from different processor architectures
are reported separately and are never combined in a summary ratio. Unless a
table states otherwise, generation and evaluation use one CPU core and a
five-second sampling target.

\subsection{Numerical validation}

For each Standard-Model process, all compared execution modes and the original
\AC{} reference are evaluated at the same deterministic phase-space point with
the same parameter card. Cross-mode comparisons use relative tolerance
$10^{-12}$ and absolute tolerance $10^{-15}$; comparisons with the independent
\AC{} result use relative tolerance $10^{-8}$. Scalar-model ladders instead
compare binary64 evaluation with higher precision and, where applicable,
direct contact-tensor contraction. Every result must reproduce its resolved
sum and preserve expected structural zeros.

Timing and numerical validation are separate requirements: a timing is
reported only when the corresponding matrix element passes all validation
checks, and numerical agreement alone does not substitute for a measured
generation or runtime value.

\clearpage
\begin{landscape}
\pagestyle{empty}
\input{section_three_mode_performance_tables.tex}
\end{landscape}
\clearpage
\pagestyle{empty}
\begin{landscape}
\section{Z-plus-Jets Evaluator Ladders}

The dedicated $d\bar d\to Z+(n-1)g$ ladders compare the independent reference
with compiled JIT O1, compiled JIT O3, ASM O3, C++ O3, eager-DAG JIT O2, and
recurrence JIT O2 evaluator configurations. They retain
final-state multiplicities $n=1,\ldots,9$ for both the built-in model and
the UFO-SM result set. Each row states its evaluator backend
and optimization level explicitly; all other workload settings follow the
benchmark contract above.

The eager and recurrence rows start from the corresponding portable JIT O2
prepared model. Each layout retains complete flow and helicity coverage; the
selected-flow workload sums helicities, while the all-flow workload selects one
helicity at runtime. Prepared-model creation is excluded from
process-generation time. Every displayed \(Z\)-ladder timing ratio uses the
matching original \AC{} workload as its denominator, while every
non-recurrence \PAC{} result is compared directly with the matching recurrence
result at the stricter cross-mode tolerance. Built-in and UFO-SM recurrence
results are likewise compared directly.

Runtime comparisons between these backends also include their vectorization
strategy. The JIT rows can use SIMD across phase-space points, whereas the
generated ASM and C++ rows are scalar. Their ratios therefore include both
code-generation quality and vectorization; they are not comparisons of
otherwise identical kernels.

\subsection*{Worked \(d\bar d\to Zg\) example}

For \(n=2\), the explicit process is
\[
  d(p_1)\,\bar d(p_2)\to Z(p_3)\,g(p_4).
\]
The report labels source particles in process order. The selected leading-colour
reference order for the selected-flow timing is therefore \((2,4,1,3)\): the
colour line enters through \(\bar d\), traverses the gluon, and exits through
\(d\), while the colour-singlet \(Z\) remains in the public ordering only to
keep source labels unambiguous. The corresponding coloured word is
\((2,4,1)\).

\PAC{} builds a shared-current representation for the selected process. Source
nodes carry particle, momentum, helicity, flavour, and colour-flow labels;
composite nodes are identified by their complete quantum state. The
selected-flow workload uses the traversal \((2,4,1)\) and sums helicities; the
all-flow workload sums every LC flow for one selected helicity. Both layouts
retain complete physical coverage and are validated against the same reference
point before timing.

\clearpage
\input{result_z_builtin_sm_table.tex}
\clearpage
\input{result_z_external_sm_table.tex}
\ReportLadderLegend
\ReportLandscapeFooter
\end{landscape}
\clearpage
\pagestyle{fancy}

\section{Colour-Singlet Model Ladders}

Both colour-singlet ladders use compiled JIT O3 evaluation and the full-colour
contract. The scalar-contact ladder retains $n=2,\ldots,8$ final scalars and
tests high-valence contact decomposition, identical-particle normalization,
and source parity without Standard-Model-specific kernels. The scalar-gravity
ladder retains $n=2,\ldots,4$ final gravitons and exercises spin-two
wavefunctions, tensor propagators, and momentum-dependent vertices. Because
every external particle in these models is a colour singlet, LC, NLC, and full
colour reduce to the same unit colour contraction.

For these ladders, the relative-difference row is
\(\bigl||\mathcal M|^2_{\mathrm{f64}}-|\mathcal M|^2_{\mathrm{hp}}\bigr|
/\max(|\mathcal M|^2_{\mathrm{hp}},10^{-300})\), where the high-precision value
uses the same phase-space point. It is a numerical stability check, not a
performance ratio.

\input{result_scalar_contact_table.tex}
\input{result_scalar_gravity_table.tex}

\section{Interpretation}

Performance comparisons are meaningful only for a baseline measured with the
same observables, parameters, selectors, and hardware profile. The result
tables therefore present validated, architecture-local comparisons; they do
not combine measurements from different machines. An \ReportNA{} entry is
outside this edition's measured scope or not applicable and must not be
interpreted as zero. Numerical agreement, timing uncertainty, and source
identity remain part of the published result set even when a compact table
shows only the central timing.

\clearpage
\input{section_zgg_example.tex}
\clearpage
\input{section_zgg_dag.tex}
\clearpage
\input{section_lc_flow_layouts.tex}
\clearpage
\input{section_eager_execution.tex}
\input{section_ufo_support.tex}

\clearpage
\begin{thebibliography}{9}

\bibitem{BerendsGiele1988}
F.~A. Berends and W.~T. Giele,
\emph{Recursive Calculations for Processes with \(n\) Gluons},
Nucl.\ Phys.\ B \textbf{306} (1988) 759--808,
doi:10.1016/0550-3213(88)90442-7.

\bibitem{Maltoni2002}
F.~Maltoni, K.~Paul, T.~Stelzer, and S.~Willenbrock,
\emph{Color-flow decomposition of QCD amplitudes},
Phys.\ Rev.\ D \textbf{67} (2003) 014026,
doi:10.1103/PhysRevD.67.014026,
arXiv:hep-ph/0209271.

\bibitem{UFO2012}
C.~Degrande, C.~Duhr, B.~Fuks, D.~Grellscheid, O.~Mattelaer, and T.~Reiter,
\emph{UFO---The Universal FeynRules Output},
Comput.\ Phys.\ Commun.\ \textbf{183} (2012) 1201--1214,
doi:10.1016/j.cpc.2012.01.022,
arXiv:1108.2040.

\bibitem{UFO2023}
L.~Darm\'e et al.,
\emph{UFO 2.0---The Universal Feynman Output format},
Eur.\ Phys.\ J.\ C \textbf{83} (2023) 631,
doi:10.1140/epjc/s10052-023-11780-9,
arXiv:2304.09883.

\bibitem{FrederixVitos2026}
R.~Frederix and T.~Vitos,
\emph{Event generation with exponential scaling in multiplicity using
AmpliCol},
JHEP \textbf{04} (2026) 192,
doi:10.1007/JHEP04(2026)192,
arXiv:2601.19483.

\end{thebibliography}

\end{document}
