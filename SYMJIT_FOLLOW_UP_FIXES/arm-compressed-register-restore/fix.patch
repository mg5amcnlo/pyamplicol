diff --git a/src/symjit/topology.rs b/src/symjit/topology.rs
--- a/src/symjit/topology.rs
+++ b/src/symjit/topology.rs
@@ -100,7 +100,7 @@ impl Topology {
 
     pub fn compile(&self, ir: &mut Mir) -> Result<()> {
         if !self.subs.is_empty() {
-            ir.branch("@success");
+            ir.branch("@normal_return");
         }
 
         for f in self.subs.values() {
diff --git a/src/symjit/builder.rs b/src/symjit/builder.rs
--- a/src/symjit/builder.rs
+++ b/src/symjit/builder.rs
@@ -375,6 +375,9 @@ impl Builder {
 
         Self::save_registers(mir, ir);
         mir.rerun(ir)?;
+        // A compressed main program skips its helpers, but must still restore
+        // the public caller's registers before entering the success epilogue.
+        ir.set_label("@normal_return");
         Self::restore_registers(mir, ir);
 
         ir.epilogue_indirect(cap, count_states, count_obs, count_params);
@@ -400,6 +403,7 @@ impl Builder {
 
         Self::save_registers(mir, ir);
         mir.rerun(ir)?;
+        ir.set_label("@normal_return");
         Self::restore_registers(mir, ir);
 
         ir.epilogue_fast(cap, count_states, count_obs, idx_ret);
diff --git a/src/symjit/arm/complex.rs b/src/symjit/arm/complex.rs
--- a/src/symjit/arm/complex.rs
+++ b/src/symjit/arm/complex.rs
@@ -722,7 +722,9 @@ impl Generator for ArmComplexGenerator {
         for r in used {
             let phys_reg = ϕ(*r);
             if (8..=15).contains(&phys_reg) {
-                self.save_stack(*r, phys_reg as u32);
+                // ABI spills are independent 64-bit values, not complex pairs.
+                // Slots 8..15 occupy bytes 64..127 of the reserved ABI area.
+                save_d_to_mem(&mut self.a, phys_reg, SP, phys_reg as u32);
             }
         }
     }
@@ -731,7 +733,7 @@ impl Generator for ArmComplexGenerator {
         for r in used {
             let phys_reg = ϕ(*r);
             if (8..=15).contains(&phys_reg) {
-                self.load_stack(*r, phys_reg as u32);
+                load_d_from_mem(&mut self.a, phys_reg, SP, phys_reg as u32);
             }
         }
     }
